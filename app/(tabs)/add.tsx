// Placeholder screen for the center + tab.
// Navigation to this screen is intercepted by the custom tabBarButton
// in _layout.tsx, which opens the global AddMovieModal instead.
export default function AddPlaceholder() {
  return null;
}
